PySide6>=6.8,<6.12
supabase>=2.20,<3
qasync>=0.27
windows-toasts>=1.3
keyring>=25
Pillow>=10

# Development / CI tooling (not imported by the app, excluded from the build).
pytest>=8
ruff>=0.6
PyInstaller>=6.11,<7
